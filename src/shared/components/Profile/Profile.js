import React from 'react';
import './Profile.css';

const Profile = () => {
    // Temp right now, once we get backend this will pull all the available data to display
    return <h1 className="profile-text">Profile</h1>
};

export default Profile;